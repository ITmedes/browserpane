# BrowserPane Claude Guide

This repo keeps its canonical project instructions in `AGENTS.md`.

Read `AGENTS.md` first and treat it as the source of truth for:
- current support matrix
- architecture map
- protocol and media assumptions
- validation commands
- contributor guardrails

This file is intentionally thin. Update `AGENTS.md` first, then adjust `CLAUDE.md` only if the Claude entrypoint needs to change.
