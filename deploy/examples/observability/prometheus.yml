global:
  scrape_interval: 15s
  scrape_timeout: 5s
  evaluation_interval: 30s

rule_files:
  - recording-rules.yml
  - alert-rules.yml

scrape_configs:
  - job_name: browserpane-gateway
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - gateway:8932
