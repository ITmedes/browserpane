# BrowserPane Recording Worker
FROM node:22-slim

RUN apt-get update && apt-get install -y \
    ca-certificates \
    chromium \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY code/integrations/recording-worker/package.json code/integrations/recording-worker/package-lock.json ./
RUN npm ci

COPY code/integrations/recording-worker/tsconfig.json ./
COPY code/integrations/recording-worker/src ./src

CMD ["npx", "tsx", "src/index.ts"]
